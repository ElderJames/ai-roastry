# 实现任务列表：DAG 工作流持久化

## 任务概述

本任务列表将设计文档转换为一系列增量实现步骤，遵循测试驱动开发（TDD）原则，确保每个步骤都能独立验证并与前序工作集成。

**优先级说明：**
- P0：核心功能，必须实现
- P1：重要功能，增强用户体验
- P2：可选功能，后续迭代

**任务重组说明：**
本任务列表已按用户需求重新组织，**优先实现 App Tool 与 Prompt 绑定功能**（第一至第三阶段），这是最核心的创新功能。数据库持久化和 Todolist 转换功能作为后续阶段实现。

---

## 🎯 第一阶段：工具参数提取服务 (P0 - 立即开始)

**目标**: 实现从 Prompt 模板自动提取参数并生成 JSON Schema

**目标**: 实现从 Prompt 模板自动提取参数并生成 JSON Schema

### 1. 实现 PromptParameterExtractor 服务
- [ ] 创建 `Services/Tools/PromptParameterExtractor### 10. 实现 AgentRoleMatcher 三级匹配策略
- [ ] 创建 `Services/Workflow/AgentRoleMatcher.cs`
  - 实现 `FindMatchingAgent(role, agents)` 主方法
  - 实现 Level 1：精确名称匹配（忽略大小写）
  - 实现 Level 2：角色字段匹配（AgentMember.Role）
  - 实现 Level 3：模糊/关键词匹配（`FindFuzzyMatch` 私有方法）
  - 实现 Level 4：单 Agent 降级匹配（仅一个 Agent 时）
  - 返回匹配结果和匹配类型（MatchType 枚举）
  - **需求参考**：US-1（智能待办任务列表创建），R-1.2（任务映射到 Agent）
- [ ] 编写单元测试
  - 测试精确匹配（"Planner" → Name="Planner"）
  - 测试角色匹配（"planner" → Role="planner"）
  - 测试模糊匹配（"搜索专家" → Role="researcher"）
  - 测试降级匹配（单 Agent 场景）
  - 测试无匹配情况（返回 null）
  - **需求参考**：NFR-4（可测试性）

### 11. 实现 DAGValidator 验证器
- [ ] 创建 `Services/Workflow/DAGValidator.cs`
  - 实现 `ValidateDAG(nodeIds, dependencies)` 主方法
  - 实现依赖存在性检查
  - 实现拓扑排序（Kahn 算法）
  - 实现循环依赖检测
  - 返回验证结果、错误列表、排序后的节点
  - **需求参考**：US-7（查看依赖关系图），R-7.3（循环检测）
- [ ] 编写单元测试
  - 测试有效 DAG（无循环）
  - 测试循环依赖检测（A → B → C → A）
  - 测试依赖不存在错误（任务依赖不存在的任务）
  - 测试拓扑排序顺序正确性
  - **需求参考**：NFR-4（可测试性）

### 12. 实现 TodolistConverter 转换器
- [ ] 创建 `Services/Workflow/TodolistConverter.cs`
  - 实现 `ConvertTodolistToDAGAsync(tasks, app)` 主方法
  - 集成 `AgentRoleMatcher` 进行任务映射
  - 加载所有 Tool App（从 ToolMetadataService）
  - 优先匹配 Tool App，其次匹配 AgentMember
  - 收集并行组信息
  - 调用 `DAGValidator` 验证 DAG
  - 返回 DAG 配置、错误列表、警告列表
  - **需求参考**：US-1（智能待办任务列表创建），R-1.3（DAG 转换）
- [ ] 编写单元测试
  - 测试成功转换（所有任务成功映射）
  - 测试 Tool App 优先匹配
  - 测试 AgentMember 匹配（精确、角色、模糊）
  - 测试映射失败错误
  - 测试 DAG 验证失败（循环依赖）
  - 测试警告生成（模糊匹配、降级匹配）
  - **需求参考**：NFR-4（可测试性）

**验收标准**：
- ✅ Todolist 成功转换为 DAG 配置
- ✅ 三级匹配策略正常工作
- ✅ 循环依赖检测有效
- ✅ 单元测试覆盖率 > 85%

---

## 第六阶段：数据库和持久化模型 (P1)

**目标**: 创建数据库表和实体类，支持 Todolist 执行历史持久化

### 13. 创建 TodolistExecution 实体类和数据库迁移
- [ ] 创建 `Data/Entities/TodolistExecution.cs` 实体类
  - 实现所有必需字段（id, app_id, session_id, status 等）
  - 添加 JSONB 字段：`TodolistJson`, `DagConfigJson`, `NodeExecutionsJson`
  - 实现 `NotMapped` 属性：`NodeExecutions`, `Todolist`（自动序列化/反序列化）
  - 添加 `TodolistStatus` 枚举（Pending, Running, Paused, Completed, Failed, Cancelled）
  - **需求参考**：US-8（工作流执行历史查询），R-8.1（执行记录持久化）

### 14. 创建 NodeExecutionRecord 模型类
- [ ] 创建 `Models/Workflow/NodeExecutionRecord.cs` JSON 模型类
  - 定义所有字段（NodeId, AgentMemberId, Status, Result 等）
  - 添加 `NodeStatus` 枚举（Pending, Running, Completed, Failed, Skipped, AwaitingInput）
  - 添加 `NodeType` 枚举（Agent, ToolApp）
  - **需求参考**：US-7（查看依赖关系图），R-7.1（DAG 可视化）

### 15. 创建并执行 EF Core 数据库迁移
- [ ] 生成迁移文件
  - 使用 `dotnet ef migrations add AddTodolistExecutionPersistence`
  - 验证生成的 SQL（仅包含 todolist_executions 表）
  - 确保包含 GIN 索引（`idx_todolist_executions_node_status`）
  - **需求参考**：R-8.1（执行记录持久化）
- [ ] 编写迁移测试
  - 测试迁移成功应用
  - 验证索引创建
  - 测试回滚功能
  - **需求参考**：NFR-4（可测试性）

**验收标准**：
- ✅ 数据库表创建成功
- ✅ JSONB 字段支持复杂对象存储
- ✅ GIN 索引创建成功

---

## 第七阶段：Todolist 执行持久化服务 (P1)

**目标**: 实现 Todolist 执行历史的 CRUD 操作

### 16. 实现 TodolistPersistenceService 基础方法s`
  - 实现 `ExtractParameters(template)` 方法
    - 使用正则表达式提取 Prompt 中的 `{{参数名}}` 占位符
    - 返回参数名称列表（去重）
    - 支持嵌套参数（如 `{{user.name}}`）
  - 实现 `GenerateParameterSchema(parameters, overrides)` 方法
    - 为每个参数生成默认 JSON Schema（type: "string", required: true）
    - 支持从 ConfigJson 的 `parameterOverrides` 覆盖参数定义
    - 返回完整的 OpenAPI 风格参数 Schema
  - 实现 `RenderPrompt(template, parameters)` 方法
    - 使用提供的参数值替换 Prompt 模板中的占位符
    - 处理缺失参数（抛出异常或使用默认值）
    - 返回渲染后的 Prompt
  - **需求参考**：US-3（App 即工具），R-3.1（参数自动提取）
  
- [ ] 编写单元测试 `PromptParameterExtractorTests.cs`
  - 测试单参数提取：`"Hello {{name}}"` → `["name"]`
  - 测试多参数提取：`"{{greeting}} {{name}}, you are {{age}} years old"` → `["greeting", "name", "age"]`
  - 测试嵌套参数：`"{{user.name}}"` → `["user.name"]`
  - 测试参数去重：`"{{name}} {{name}}"` → `["name"]`
  - 测试 Schema 生成（默认类型 string）
  - 测试 Schema 覆盖（从 parameterOverrides 读取自定义类型）
  - 测试 Prompt 渲染成功
  - 测试 Prompt 渲染缺失参数时的错误处理
  - **需求参考**：NFR-4（可测试性）

**验收标准**：
- ✅ 能够从任意 Prompt 模板中提取所有参数
- ✅ 生成的 JSON Schema 符合 OpenAPI 3.0 规范
- ✅ 单元测试覆盖率 > 90%

---

## 🎯 第二阶段：工具元数据管理服务 (P0)

**目标**: 使用 HybridCache 缓存所有 App Tool 和 MCP Tool 的元数据

### 2. 创建 ToolMetadata 模型类
- [ ] 创建 `Models/Tools/ToolMetadata.cs`
  - 定义字段：
    ```csharp
    public string Name { get; set; }              // 工具名称（唯一）
    public string Description { get; set; }       // 工具描述
    public ToolSource Source { get; set; }        // 来源：App 或 MCP
    public Guid SourceId { get; set; }            // 源 ID（AppId 或 McpServerId）
    public string ParametersSchema { get; set; }  // JSON Schema（JSON 字符串）
    public string? ConfigJson { get; set; }       // 原始配置（可选）
    ```
  - 添加 `ToolSource` 枚举
    ```csharp
    public enum ToolSource { App, MCP }
    ```
  - **需求参考**：US-4（多源工具管理），R-4.1（统一工具接口）

### 3. 实现 ToolMetadataService
- [ ] 创建 `Services/Tools/ToolMetadataService.cs`
  - 注入依赖：`HybridCache`, `ApplicationDbContext`, `PromptParameterExtractor`
  
  - 实现 `InitializeAsync()` 启动扫描方法
    - 调用 `ScanAppToolsAsync()` 扫描所有 App Tool
    - 调用 `ScanMcpToolsAsync()` 扫描所有 MCP Tool（暂时 TODO 占位）
    - 记录扫描日志（工具数量、耗时）
  
  - 实现 `ScanAppToolsAsync()` 扫描 App Tool 方法
    - 查询数据库：`WHERE AppType = "Tool" AND IsActive = true`
    - 对每个 Tool App：
      - 从 `PromptTemplate` 提取参数
      - 从 `ConfigJson` 读取 `parameterOverrides`
      - 生成 ToolMetadata 对象
      - 写入 HybridCache（键：`tool_metadata:{Name}`）
    - 汇总所有工具列表，写入缓存（键：`tool_metadata:all`）
  
  - 实现 `ScanMcpToolsAsync()` 扫描 MCP Tool 方法（TODO 占位）
    - 添加注释：`// TODO: 集成 MCP SDK 后实现`
    - 暂时返回空列表
  
  - 实现 `GetAllToolsAsync()` 获取所有工具方法
    - 从缓存读取：`tool_metadata:all`
    - 如果缓存未命中，调用 `InitializeAsync()` 重新扫描
    - 返回 `List<ToolMetadata>`
  
  - 实现 `GetToolByNameAsync(name)` 按名称查找方法
    - 从缓存读取：`tool_metadata:{name}`
    - 如果未找到，返回 `null`
  
  - **需求参考**：US-4（多源工具管理），R-4.2（工具发现机制）

- [ ] 编写单元测试 `ToolMetadataServiceTests.cs`
  - 使用内存数据库模拟 Tool App 数据
  - 测试 `ScanAppToolsAsync()` 扫描成功
    - 创建 3 个 Tool App（不同 Prompt 模板）
    - 验证扫描后缓存包含 3 个工具
    - 验证每个工具的 ParametersSchema 正确
  - 测试缓存写入和读取
    - 调用 `GetAllToolsAsync()` 验证返回所有工具
    - 调用 `GetToolByNameAsync("tool1")` 验证返回正确工具
  - 测试缓存未命中时的重新扫描
    - 清空缓存，调用 `GetAllToolsAsync()` 验证自动重新扫描
  - **需求参考**：NFR-4（可测试性）

### 4. 实现工具元数据缓存刷新机制
- [ ] 在 `ToolMetadataService` 中实现 `RefreshAppToolAsync(appId)`
  - 根据 `appId` 查询数据库获取 Tool App
  - 如果 App 不存在或非 Tool 类型，移除缓存并返回
  - 重新提取参数和生成 ToolMetadata
  - 移除旧缓存键：`await _cache.RemoveAsync($"tool_metadata:{oldName}")`
  - 写入新缓存：`await _cache.SetAsync($"tool_metadata:{newName}", metadata)`
  - 刷新全局列表缓存：`await _cache.RemoveAsync("tool_metadata:all")`
  - **需求参考**：R-4.3（缓存同步）

- [ ] 在 `ToolMetadataService` 中实现 `RefreshMcpServerAsync(serverId)`
  - 查询该 MCP Server 的所有工具（TODO：集成 MCP SDK 后实现）
  - 移除所有相关缓存键
  - 重新扫描并缓存
  - **需求参考**：R-4.3（缓存同步）

- [ ] 编写集成测试 `ToolMetadataServiceIntegrationTests.cs`
  - 测试 App 更新后缓存自动刷新
    - 创建 Tool App，验证缓存存在
    - 修改 App 的 Prompt 模板（添加新参数）
    - 调用 `RefreshAppToolAsync(appId)`
    - 验证缓存已更新，新参数已包含在 Schema 中
  - 测试 App 删除或改为非 Tool 类型后缓存清除
  - **需求参考**：NFR-4（可测试性）

### 5. 配置 HybridCache 和应用启动初始化
- [ ] 在 `Program.cs` 中配置 HybridCache
  ```csharp
  builder.Services.AddHybridCache(options =>
  {
      options.MaximumPayloadBytes = 1024 * 1024; // 1MB
      options.MaximumKeyLength = 512;
      options.DefaultEntryOptions = new HybridCacheEntryOptions
      {
          Expiration = TimeSpan.FromHours(24),              // Redis 缓存 24 小时
          LocalCacheExpiration = TimeSpan.FromMinutes(30)   // 本地缓存 30 分钟
      };
  });
  ```
  - 添加配置选项到 `appsettings.json`
  ```json
  "HybridCache": {
    "ExpirationHours": 24,
    "LocalExpirationMinutes": 30
  }
  ```
  - **需求参考**：NFR-3（可扩展性）

- [ ] 在应用启动时初始化工具元数据
  ```csharp
  var toolMetadataService = app.Services.GetRequiredService<ToolMetadataService>();
  await toolMetadataService.InitializeAsync();
  _logger.LogInformation("Tool metadata initialized: {Count} tools loaded", 
      (await toolMetadataService.GetAllToolsAsync()).Count);
  ```
  - 添加错误处理（初始化失败不应阻止应用启动）
  - **需求参考**：R-4.2（工具发现机制）

- [ ] 编写启动集成测试
  - 测试应用启动时工具元数据初始化成功
  - 测试初始化失败时的降级处理（记录错误，继续启动）
  - **需求参考**：NFR-4（可测试性）

**验收标准**：
- ✅ 应用启动时自动扫描并缓存所有 Tool App
- ✅ HybridCache 命中率 > 90%（本地缓存）
- ✅ App 配置更新时缓存自动刷新
- ✅ 单元测试和集成测试覆盖率 > 85%

---

## 🎯 第三阶段：App Tool 动态注册和执行 (P0 - 核心功能)

**目标**: 将 App Tool 注册到 Semantic Kernel，支持其他 Agent 调用

### 6. 实现 AppToolPlugin 插件
- [ ] 创建 `Services/Tools/AppToolPlugin.cs`
  - 注入依赖：`PromptParameterExtractor`, `AgentOrchestratorService`, `ApplicationDbContext`
  
  - 实现 `RegisterToolApp(kernel, toolApp)` 静态方法
    - 从 ToolApp 提取参数 Schema
    - 使用 `kernel.CreateFunctionFromMethod()` 动态创建 KernelFunction
    - 函数逻辑：调用 `ExecuteToolAppAsync(toolApp, arguments)`
    - 添加函数元数据（Description, Parameters）
    - **需求参考**：US-3（App 即工具），R-3.2（工具注册）
  
  - 实现 `ExecuteToolAppAsync(toolApp, arguments)` 私有方法
    - 步骤 1：渲染 Prompt 模板
      ```csharp
      var prompt = _parameterExtractor.RenderPrompt(toolApp.PromptTemplate, arguments);
      ```
    - 步骤 2：调用 AgentOrchestratorService 执行
      ```csharp
      var result = await _orchestrator.ExecuteAsync(new ExecuteRequest
      {
          AppId = toolApp.Id,
          UserMessages = new[] { new Message { Role = "user", Content = prompt } },
          SessionId = Guid.NewGuid().ToString() // 每次调用独立会话
      });
      ```
    - 步骤 3：返回 LLM 响应结果（最后一条 Assistant 消息）
    - **需求参考**：R-3.3（工具执行）
  
  - 处理参数覆盖（从 ConfigJson 读取 `parameterOverrides`）
    ```csharp
    var config = JsonSerializer.Deserialize<ToolAppConfig>(toolApp.ConfigJson);
    var overrides = config?.ParameterOverrides ?? new Dictionary<string, object>();
    ```
  
- [ ] 编写单元测试 `AppToolPluginTests.cs`
  - 测试工具注册
    - 创建模拟 Tool App（Prompt: `"Translate '{{text}}' to {{language}}"`）
    - 调用 `RegisterToolApp(kernel, toolApp)`
    - 验证 Kernel 中存在名为 `toolApp.Name` 的函数
    - 验证函数参数包含 `text` 和 `language`
  - 测试参数提取和 Prompt 渲染
    - 模拟工具调用：`arguments = { "text": "Hello", "language": "Chinese" }`
    - 验证生成的 Prompt：`"Translate 'Hello' to Chinese"`
  - 测试工具执行（模拟 Orchestrator 调用）
    - Mock `AgentOrchestratorService.ExecuteAsync()`
    - 返回模拟响应：`"你好"`
    - 验证 `ExecuteToolAppAsync()` 返回 `"你好"`
  - 测试错误处理
    - App 不存在时抛出异常
    - Prompt 模板为空时抛出异常
    - 参数缺失时抛出异常
  - **需求参考**：NFR-4（可测试性）

### 7. 集成 App Tool 到 Kernel 注册流程
- [ ] 在应用启动时注册所有 Tool App
  ```csharp
  // 在 Program.cs 中（工具元数据初始化后）
  var toolMetadataService = app.Services.GetRequiredService<ToolMetadataService>();
  var appTools = (await toolMetadataService.GetAllToolsAsync())
      .Where(t => t.Source == ToolSource.App)
      .ToList();
  
  var kernel = app.Services.GetRequiredService<Kernel>();
  var db = app.Services.GetRequiredService<ApplicationDbContext>();
  
  foreach (var toolMetadata in appTools)
  {
      var toolApp = await db.LlmApps.FindAsync(toolMetadata.SourceId);
      if (toolApp != null)
      {
          AppToolPlugin.RegisterToolApp(kernel, toolApp);
          _logger.LogInformation("Registered App Tool: {Name} ({ParamCount} parameters)", 
              toolApp.Name, toolMetadata.ParametersSchema.Split(',').Length);
      }
  }
  ```
  - **需求参考**：R-3.2（工具注册）

- [ ] 编写端到端集成测试 `AppToolIntegrationTests.cs`
  - 测试场景：创建 Tool App → 注册到 Kernel → 从 Agent 调用
    - 步骤 1：创建测试 Tool App
      ```csharp
      var toolApp = new LlmApp
      {
          Name = "Calculator",
          AppType = "Tool",
          PromptTemplate = "Calculate: {{expression}}",
          // ... 其他字段
      };
      await _db.LlmApps.AddAsync(toolApp);
      await _db.SaveChangesAsync();
      ```
    - 步骤 2：调用 `ToolMetadataService.ScanAppToolsAsync()` 刷新缓存
    - 步骤 3：调用 `AppToolPlugin.RegisterToolApp(kernel, toolApp)` 注册工具
    - 步骤 4：创建测试 Agent App（包含 Calculator 工具）
    - 步骤 5：通过 `/v1/chat/completions` 调用 Agent
      ```json
      {
        "model": "agent-app-id",
        "messages": [{"role": "user", "content": "What is 123 + 456?"}]
      }
      ```
    - 步骤 6：验证响应中包含工具调用
      ```json
      {
        "tool_calls": [{
          "function": {"name": "Calculator", "arguments": "{\"expression\":\"123+456\"}"}
        }]
      }
      ```
    - 步骤 7：验证最终响应：`"The result is 579"`
  - **需求参考**：NFR-4（可测试性）

**验收标准**：
- ✅ 所有 Tool App 在应用启动时自动注册到 Kernel
- ✅ 从 Agent 调用 Tool App 成功，参数正确传递
- ✅ Tool App 返回的结果能被 Agent 正确处理
- ✅ 端到端测试通过

---

## 第四阶段：基础数据模型（仅必要部分）(P0)

**目标**: 创建 Todolist 相关的基础模型类，为后续转换功能提供数据结构

### 8. 创建 Todolist 相关模型类
- [ ] 创建 `Models/Workflow/TodolistTask.cs` 模型类
  - 定义字段：TaskId, Title, Description, AssignedAgentRole, Dependencies 等
  - **需求参考**：US-1（智能待办任务列表创建），R-1.1（Todolist 创建工具）
- [ ] 创建 `Models/Workflow/CreateTodolistRequest.cs` 和 `CreateTodolistResponse.cs`
  - Request: SessionId, UserRequest, SuggestedTasks
  - Response: Success, ExecutionId, ValidationWarnings, Errors
  - **需求参考**：US-1（智能待办任务列表创建）

### 9. 创建 DAG 转换相关模型类
- [ ] 创建 `Models/Workflow/DAGNodeConfig.cs`
  - 定义节点配置（NodeId, NodeType, AgentMemberId, ToolAppId, Dependencies 等）
  - **需求参考**：US-7（查看依赖关系图），R-7.2（节点依赖关系）
- [ ] 创建 `Models/Workflow/ConvertedDAGConfig.cs`
  - 定义转换后的 DAG 配置（Nodes, ParallelGroups, ExecutionOrder）
  - **需求参考**：US-7（查看依赖关系图）

**验收标准**：
- ✅ 模型类定义完整，字段类型正确
- ✅ 支持 JSON 序列化/反序列化

---

## 第五阶段：任务映射和 Todolist 转换 (P0)

**目标**: 实现 Todolist 到 DAG 的智能转换，包含三级 Agent 匹配策略

### 10. 实现 AgentRoleMatcher 三级匹配策略
  - 实现所有必需字段（id, app_id, session_id, status 等）
  - 添加 JSONB 字段：`TodolistJson`, `DagConfigJson`, `NodeExecutionsJson`
  - 实现 `NotMapped` 属性：`NodeExecutions`, `Todolist`（自动序列化/反序列化）
  - 添加 `TodolistStatus` 枚举（Pending, Running, Paused, Completed, Failed, Cancelled）
  - **需求参考**：US-8（工作流执行历史查询），R-8.1（执行记录持久化）

### 2. 创建 NodeExecutionRecord 和相关模型类
- [ ] 创建 `NodeExecutionRecord.cs` JSON 模型类
  - 定义所有字段（NodeId, AgentMemberId, Status, Result 等）
  - 添加 `NodeStatus` 枚举（Pending, Running, Completed, Failed, Skipped, AwaitingInput）
  - 添加 `NodeType` 枚举（Agent, ToolApp）
  - **需求参考**：US-7（查看依赖关系图），R-7.1（DAG 可视化）

### 3. 创建 Todolist 相关模型类
- [ ] 创建 `TodolistTask.cs` 模型类
  - 定义字段：TaskId, Title, Description, AssignedAgentRole, Dependencies 等
  - **需求参考**：US-1（智能待办任务列表创建），R-1.1（Todolist 创建工具）
- [ ] 创建 `CreateTodolistRequest/Response` 模型类
  - 定义请求和响应结构
  - 包含验证警告列表
  - **需求参考**：US-1（智能待办任务列表创建）

### 4. 创建 DAG 转换相关模型类
- [ ] 创建 `DAGNodeConfig.cs` 和 `ConvertedDAGConfig.cs`
  - 定义节点配置（NodeId, NodeType, AgentMemberId, ToolAppId, Dependencies 等）
  - 定义转换后的 DAG 配置（Nodes, ParallelGroups, ExecutionOrder）
  - **需求参考**：US-7（查看依赖关系图），R-7.2（节点依赖关系）

### 5. 创建并执行 EF Core 数据库迁移
- [ ] 生成迁移文件
  - 使用 `dotnet ef migrations add AddTodolistExecutionPersistence`
  - 验证生成的 SQL（仅包含 todolist_executions 表）
  - 确保包含 GIN 索引（`idx_todolist_executions_node_status`）
  - **需求参考**：R-8.1（执行记录持久化）
- [ ] 编写迁移测试
  - 测试迁移成功应用
  - 验证索引创建
  - 测试回滚功能
  - **需求参考**：NFR-4（可测试性）

---

## 第二阶段：工具元数据管理服务 (P0)

### 6. 实现 PromptParameterExtractor 服务
- [ ] 创建 `PromptParameterExtractor.cs`
  - 实现 `ExtractParameters(template)` 方法（正则提取 `{{参数}}`）
  - 实现 `GenerateParameterSchema(parameters, overrides)` 方法
  - 实现 `RenderPrompt(template, parameters)` 方法
  - **需求参考**：US-3（App 即工具），R-3.1（参数自动提取）
- [ ] 编写单元测试
  - 测试参数提取（单参数、多参数、嵌套参数）
  - 测试 Schema 生成（默认类型、参数覆盖）
  - 测试 Prompt 渲染（完整替换、缺失参数处理）
  - **需求参考**：NFR-4（可测试性）

### 7. 实现 ToolMetadata 模型和 ToolMetadataService
- [ ] 创建 `ToolMetadata.cs` 内存模型类
  - 定义字段（Name, Description, Source, SourceId, ParametersSchema 等）
  - 添加 `ToolSource` 枚举（App, MCP）
  - **需求参考**：US-4（多源工具管理），R-4.1（统一工具接口）
- [ ] 创建 `ToolMetadataService.cs`
  - 实现 `InitializeAsync()` 启动扫描方法
  - 实现 `ScanAppToolsAsync()` 扫描 App Tool 方法
  - 实现 `ScanMcpToolsAsync()` 扫描 MCP Tool 方法（TODO 占位）
  - 实现 `GetAllToolsAsync()` 获取所有工具方法
  - 实现 `GetToolByNameAsync(name)` 按名称查找方法
  - 使用 HybridCache 缓存（键格式：`tool_metadata:{name}`, `tool_metadata:all`）
  - **需求参考**：US-4（多源工具管理），R-4.2（工具发现机制）
- [ ] 编写单元测试
  - 测试 App Tool 扫描（模拟数据库返回多个 Tool App）
  - 测试缓存写入和读取
  - 测试缓存过期和自动重建
  - **需求参考**：NFR-4（可测试性）

### 8. 实现工具元数据缓存刷新机制
- [ ] 在 `ToolMetadataService` 中实现 `RefreshAppToolAsync(appId)`
  - 移除旧缓存键
  - 重新扫描单个 App Tool
  - 刷新全局工具列表缓存
  - **需求参考**：R-4.3（缓存同步）
- [ ] 在 `ToolMetadataService` 中实现 `RefreshMcpServerAsync(serverId)`
  - 移除 MCP Server 所有工具的缓存
  - 重新扫描 MCP Server
  - 刷新全局工具列表缓存
  - **需求参考**：R-4.3（缓存同步）
- [ ] 编写集成测试
  - 测试 App 更新后缓存自动刷新
  - 测试 MCP 配置更新后缓存刷新
  - 验证缓存一致性
  - **需求参考**：NFR-4（可测试性）

### 9. 配置 HybridCache 和应用启动初始化
- [ ] 在 `Program.cs` 中配置 HybridCache
  - 添加 `builder.Services.AddHybridCache()` 配置
  - 设置过期时间（24 小时分布式，30 分钟本地）
  - 添加配置选项到 `appsettings.json`
  - **需求参考**：NFR-3（可扩展性）
- [ ] 在应用启动时初始化工具元数据
  - 在 `Program.cs` 中调用 `ToolMetadataService.InitializeAsync()`
  - 添加启动日志记录
  - 处理初始化失败场景
  - **需求参考**：R-4.2（工具发现机制）
- [ ] 编写启动集成测试
  - 测试应用启动时工具元数据初始化成功
  - 测试初始化失败时的降级处理
  - **需求参考**：NFR-4（可测试性）

---

## 第三阶段：App Tool 注册和执行 (P0)

### 10. 实现 AppToolPlugin 插件
- [ ] 创建 `AppToolPlugin.cs`
  - 实现 `RegisterToolApp(kernel, toolApp)` 方法（动态注册到 SK）
  - 实现 `ExecuteToolAppAsync(toolApp, arguments)` 方法
    - 渲染 Prompt 模板
    - 调用 AgentOrchestratorService 执行
    - 返回 LLM 响应结果
  - 处理参数覆盖（从 ConfigJson 读取 `parameterOverrides`）
  - **需求参考**：US-3（App 即工具），R-3.2（工具注册）
- [ ] 编写单元测试
  - 测试工具注册（验证 Kernel Function 创建）
  - 测试参数提取和 Prompt 渲染
  - 测试工具执行（模拟 Orchestrator 调用）
  - 测试错误处理（App 不存在、Prompt 缺失等）
  - **需求参考**：NFR-4（可测试性）

### 11. 集成 App Tool 到 Kernel 注册流程
- [ ] 在应用启动时注册所有 Tool App
  - 从 `ToolMetadataService` 获取所有 App Tool
  - 调用 `AppToolPlugin.RegisterToolApp()` 注册到全局 Kernel
  - 添加日志记录（工具名称、参数数量）
  - **需求参考**：R-3.2（工具注册）
- [ ] 编写集成测试
  - 创建测试 Tool App（带 Prompt 模板）
  - 验证工具成功注册到 Kernel
  - 测试从 Agent 调用 Tool App
  - 验证参数正确传递和响应返回
  - **需求参考**：NFR-4（可测试性）

---

## 第四阶段：任务映射和 Todolist 转换 (P0)

### 12. 实现 AgentRoleMatcher 三级匹配策略
- [ ] 创建 `AgentRoleMatcher.cs`
  - 实现 `FindMatchingAgent(role, agents)` 主方法
  - 实现 Level 1：精确名称匹配（忽略大小写）
  - 实现 Level 2：角色字段匹配（AgentMember.Role）
  - 实现 Level 3：模糊/关键词匹配（`FindFuzzyMatch` 私有方法）
  - 实现 Level 4：单 Agent 降级匹配（仅一个 Agent 时）
  - 返回匹配结果和匹配类型（MatchType 枚举）
  - **需求参考**：US-1（智能待办任务列表创建），R-1.2（任务映射到 Agent）
- [ ] 编写单元测试
  - 测试精确匹配（"Planner" → Name="Planner"）
  - 测试角色匹配（"planner" → Role="planner"）
  - 测试模糊匹配（"搜索专家" → Role="researcher"）
  - 测试降级匹配（单 Agent 场景）
  - 测试无匹配情况（返回 null）
  - **需求参考**：NFR-4（可测试性）

### 13. 实现 DAGValidator 验证器
- [ ] 创建 `DAGValidator.cs`
  - 实现 `ValidateDAG(nodeIds, dependencies)` 主方法
  - 实现依赖存在性检查
  - 实现拓扑排序（Kahn 算法）
  - 实现循环依赖检测
  - 返回验证结果、错误列表、排序后的节点
  - **需求参考**：US-7（查看依赖关系图），R-7.3（循环检测）
- [ ] 编写单元测试
  - 测试有效 DAG（无循环）
  - 测试循环依赖检测（A → B → C → A）
  - 测试依赖不存在错误（任务依赖不存在的任务）
  - 测试拓扑排序顺序正确性
  - **需求参考**：NFR-4（可测试性）

### 14. 实现 TodolistConverter 转换器
- [ ] 创建 `TodolistConverter.cs`
  - 实现 `ConvertTodolistToDAGAsync(tasks, app)` 主方法
  - 集成 `AgentRoleMatcher` 进行任务映射
  - 加载所有 Tool App（从数据库）
  - 优先匹配 Tool App，其次匹配 AgentMember
  - 收集并行组信息
  - 调用 `DAGValidator` 验证 DAG
  - 返回 DAG 配置、错误列表、警告列表
  - **需求参考**：US-1（智能待办任务列表创建），R-1.3（DAG 转换）
- [ ] 编写单元测试
  - 测试成功转换（所有任务成功映射）
  - 测试 Tool App 优先匹配
  - 测试 AgentMember 匹配（精确、角色、模糊）
  - 测试映射失败错误
  - 测试 DAG 验证失败（循环依赖）
  - 测试警告生成（模糊匹配、降级匹配）
  - **需求参考**：NFR-4（可测试性）

---

## 第五阶段：Todolist 执行持久化服务 (P0)

### 15. 实现 TodolistPersistenceService 基础方法
- [ ] 创建 `TodolistPersistenceService.cs`
  - 实现 `CreateTodolistExecutionAsync()` 方法（Fire-and-forget）
    - 创建 TodolistExecution 实体
    - 序列化 Todolist、DAG Config、Node Executions 到 JSONB
    - 初始化所有节点状态为 Pending
    - 异步写入数据库（Task.Run）
    - 添加错误处理和日志记录
  - **需求参考**：US-8（工作流执行历史查询），R-8.1（执行记录持久化）
- [ ] 编写单元测试
  - 测试 Todolist 执行记录创建成功
  - 测试 JSONB 序列化正确性
  - 测试异步写入不阻塞调用线程
  - 测试数据库写入失败时的错误处理
  - **需求参考**：NFR-4（可测试性）

### 16. 实现 TodolistPersistenceService 基础方法
- [ ] 创建 `Services/Workflow/TodolistPersistenceService.cs`
  - 实现 `CreateTodolistExecutionAsync()` 方法（Fire-and-forget）
  - 实现 `UpdateTodolistStatusAsync()` 方法
  - 实现 `UpdateNodeStatusAsync()` 方法
  - **需求参考**：US-8（工作流执行历史查询），R-8.1-8.2

### 17. 实现 Todolist 执行查询方法
- [ ] 实现 `GetTodolistHistoryAsync()` - 支持过滤和分页
- [ ] 实现 `GetTodolistDetailsAsync()` - 查询完整执行记录
- [ ] 实现 `GetNodesByStatusAsync()` - JSONB 查询
- [ ] 编写集成测试
  - **需求参考**：R-8.3（历史查询）

---

## 第八阶段：工作流管理工具插件 (P1)

**目标**: 实现 8 个工作流管理工具

### 18-23. 实现工作流管理工具
- [ ] create_workflow_todolist
- [ ] get_workflow_history 和 get_workflow_details
- [ ] get_dependency_graph
- [ ] pause_workflow 和 resume_workflow
- [ ] retry_failed_nodes 和 modify_workflow_tasks
- **需求参考**：US-1, US-7, US-8, US-9, US-10, US-11

---

## 第九阶段：DAGStrategy 集成 (P2)

### 24-25. DAGStrategy 集成持久化和暂停恢复
- [ ] 扩展 DAGStrategy 支持持久化
- [ ] 实现暂停和恢复机制
- **需求参考**：US-8, US-9

---

## 第十阶段：UI、测试和文档 (P2)

### 26-33. UI 组件、E2E 测试、文档
- [ ] 创建 UI 可视化组件（TodolistHistoryList, DependencyGraph 等）
- [ ] 编写端到端集成测试
- [ ] 性能和负载测试
- [ ] 更新配置文件和文档

---

## 📊 任务完成标准

### 第一阶段（App Tool）完成标准
- ✅ 所有 Tool App 在应用启动时自动注册到 Kernel
- ✅ 从 Agent 调用 Tool App 成功，参数正确传递
- ✅ HybridCache 缓存命中率 > 90%
- ✅ 单元测试和集成测试覆盖率 > 85%
- ✅ 端到端测试：创建 Tool App → 注册 → 从 Agent 调用 → 验证响应

### 最终验收（所有阶段完成后）
- ✅ 所有 13 个用户故事（US-1 到 US-13）均已实现并通过测试
- ✅ 8 个工作流管理工具可通过 `/v1/chat/completions` 调用
- ✅ App Tool 可以作为可重用工具被其他 Agent 调用
- ✅ Todolist 可以成功转换为 DAG 并执行
- ✅ 所有单元测试通过（覆盖率 > 80%）
- ✅ 端到端测试覆盖所有主要用户场景

---

**任务总数：** 33 个主任务（重新排序）
**第一阶段估计时间：** 2-3 个工作日（任务 1-7）
**总估计时间：** 6-8 个工作日
**依赖关系：** 第一至三阶段（App Tool 功能）独立可用，第四至十阶段依赖前序完成
**风险点：** Prompt 参数提取的正则表达式复杂度、HybridCache 配置

---

## 🚀 推荐执行顺序

**优先执行（P0）：**
1. 任务 1-7：App Tool 与 Prompt 绑定（第一至三阶段）
2. 任务 8-12：Todolist 转换功能（第四至五阶段）

**后续执行（P1-P2）：**
3. 任务 13-17：数据库持久化（第六至七阶段）
4. 任务 18-33：工作流管理、集成、UI、文档（第八至十阶段）
  - 更新 Status, CompletedAt, ErrorMessage, FinalResult
  - 更新 UpdatedAt 时间戳
  - **需求参考**：R-8.2（状态更新）
- [ ] 在 `TodolistPersistenceService` 中实现 `UpdateNodeStatusAsync()`
  - 反序列化 `NodeExecutionsJson`
  - 查找并更新指定节点状态
  - 序列化回 JSONB
  - 更新 UpdatedAt 时间戳
  - **需求参考**：R-8.2（状态更新）
- [ ] 编写单元测试
  - 测试 Todolist 执行状态更新
  - 测试节点状态更新（JSONB 数组操作）
  - 测试节点不存在时的错误处理
  - 验证时间戳自动更新
  - **需求参考**：NFR-4（可测试性）

### 17. 实现 Todolist 执行查询和批量更新方法
- [ ] 在 `TodolistPersistenceService` 中实现 `GetTodolistHistoryAsync()`
  - 支持按 sessionId, status 过滤
  - 支持分页（skip, take）
  - 按创建时间倒序排序
  - **需求参考**：US-8（工作流执行历史查询），R-8.3（历史查询）
- [ ] 在 `TodolistPersistenceService` 中实现 `GetTodolistDetailsAsync()`
  - 按 executionId 查询完整 Todolist 执行记录
  - 自动反序列化 JSONB 字段
  - **需求参考**：US-8（工作流执行历史查询），R-8.3（历史查询）
- [ ] 在 `TodolistPersistenceService` 中实现 `BatchUpdateNodeStatusAsync()`
  - 使用事务保证一致性
  - 批量更新多个节点状态
  - 返回更新结果（成功、失败、更新数量）
  - **需求参考**：R-8.2（状态更新）
- [ ] 编写集成测试
  - 测试历史查询（过滤、分页）
  - 测试详细信息查询
  - 测试批量更新事务
  - 测试批量更新失败回滚
  - **需求参考**：NFR-4（可测试性）

### 18. 实现 PostgreSQL JSONB 查询方法
- [ ] 在 `TodolistPersistenceService` 中实现 `GetNodesByStatusAsync()`
  - 使用原生 SQL 查询（`@>` JSONB 运算符）
  - 过滤特定状态的节点
  - 验证 GIN 索引使用
  - **需求参考**：R-8.3（历史查询）
- [ ] 编写性能测试
  - 测试 JSONB 查询性能（对比 JOIN 查询）
  - 验证 GIN 索引生效
  - 测试大数据量场景（100+ Todolist 执行）
  - **需求参考**：NFR-2（可观察性）

---

## 第六阶段：工作流管理插件（8 个工具）(P0)

### 19. 实现 create_workflow_todolist 工具
- [ ] 在 `WorkflowManagementPlugin` 中实现 `CreateWorkflowTodolistAsync()`
  - 接收参数：sessionId, userRequest, suggestedTasks
  - 调用 `TodolistConverter.ConvertTodolistToDAGAsync()`
  - 验证 DAG 有效性
  - 调用 `TodolistPersistenceService.CreateTodolistExecutionAsync()`
  - 返回 CreateTodolistResponse（成功、executionId、验证警告）
  - 添加 `[KernelFunction]` 和 `[Description]` 特性
  - **需求参考**：US-1（智能待办任务列表创建），R-1.1（Todolist 创建工具）
- [ ] 编写集成测试
  - 测试端到端创建流程（输入 → 转换 → 验证 → 持久化）
  - 测试验证失败场景（循环依赖、无效映射）
  - 测试警告返回（模糊匹配警告）
  - **需求参考**：NFR-4（可测试性）

### 20. 实现 get_workflow_history 和 get_workflow_details 工具
- [ ] 在 `WorkflowManagementPlugin` 中实现 `GetWorkflowHistoryAsync()`
  - 接收参数：sessionId, appId, status, limit, offset
  - 调用 `TodolistPersistenceService.GetTodolistHistoryAsync()`
  - 格式化返回结果（包含节点统计：total, completed, failed, running, pending）
  - **需求参考**：US-8（工作流执行历史查询），R-8.3（历史查询）
- [ ] 在 `WorkflowManagementPlugin` 中实现 `GetWorkflowDetailsAsync()`
  - 接收参数：executionId
  - 调用 `TodolistPersistenceService.GetTodolistDetailsAsync()`
  - 返回完整 Todolist 执行信息（包含 Todolist、DAG Config、Node Executions）
  - **需求参考**：US-8（工作流执行历史查询），R-8.3（历史查询）
- [ ] 编写单元测试
  - 测试历史查询（过滤、分页、结果格式化）
  - 测试详细信息查询
  - **需求参考**：NFR-4（可测试性）

### 21. 实现 get_dependency_graph 工具
- [ ] 在 `WorkflowManagementPlugin` 中实现 `GetDependencyGraphAsync()`
  - 接收参数：executionId
  - 查询 Todolist 执行详细信息
  - 提取 DAG Config 中的依赖关系
  - 生成 Mermaid 格式的依赖图（或 JSON 格式）
  - 返回可视化数据
  - **需求参考**：US-7（查看依赖关系图），R-7.1（DAG 可视化）
- [ ] 编写单元测试
  - 测试依赖图生成（简单 DAG、复杂 DAG）
  - 测试并行组标注
  - 测试节点状态颜色标记
  - **需求参考**：NFR-4（可测试性）

### 22. 实现 pause_workflow 和 resume_workflow 工具
- [ ] 在 `WorkflowManagementPlugin` 中实现 `PauseWorkflowAsync()`
  - 接收参数：executionId
  - 更新 Todolist 执行状态为 Paused
  - 停止 DAGStrategy 执行（需要集成 DAGStrategy）
  - 返回操作结果
  - **需求参考**：US-9（工作流暂停和恢复），R-9.1（暂停工具）
- [ ] 在 `WorkflowManagementPlugin` 中实现 `ResumeWorkflowAsync()`
  - 接收参数：executionId
  - 更新 Todolist 执行状态为 Running
  - 恢复 DAGStrategy 执行
  - 返回操作结果
  - **需求参考**：US-9（工作流暂停和恢复），R-9.2（恢复工具）
- [ ] 编写集成测试
  - 测试暂停和恢复流程
  - 测试状态验证（只能暂停 Running 的 Todolist 执行）
  - **需求参考**：NFR-4（可测试性）

### 23. 实现 retry_failed_nodes 和 modify_workflow_tasks 工具
- [ ] 在 `WorkflowManagementPlugin` 中实现 `RetryFailedNodesAsync()`
  - 接收参数：executionId
  - 查询所有 Failed 状态的节点
  - 重置节点状态为 Pending
  - 触发 DAGStrategy 重新执行
  - 返回重试节点列表
  - **需求参考**：US-10（失败节点重试），R-10.1（重试工具）
- [ ] 在 `WorkflowManagementPlugin` 中实现 `ModifyWorkflowTasksAsync()`
  - 接收参数：sessionId, modifications
  - 查询当前会话的 Todolist 执行
  - 更新 TodolistJson 和 NodeExecutionsJson
  - 重新验证 DAG
  - 返回修改结果
  - 会话隔离验证
  - **需求参考**：US-11（动态任务修改），R-11.1（修改工具）
- [ ] 编写集成测试
  - 测试失败节点重试
  - 测试任务修改（添加、删除、更新）
  - 测试会话隔离（只能修改当前会话的 Todolist 执行）
  - **需求参考**：NFR-4（可测试性）

---

## 第七阶段：DAGStrategy 集成持久化 (P1)

### 24. 扩展 DAGStrategy 支持 Todolist 执行持久化
- [ ] 修改 `DAGStrategy` 执行流程
  - 在执行开始时调用 `TodolistPersistenceService.CreateTodolistExecutionAsync()`
  - 在每个节点执行前更新节点状态为 Running
  - 在每个节点执行后更新节点状态（Completed/Failed）
  - 在 Todolist 执行完成时更新执行状态
  - 添加 executionId 参数传递
  - **需求参考**：US-8（工作流执行历史查询），R-8.1（执行记录持久化）
- [ ] 编写集成测试
  - 测试完整的 DAG 执行并持久化
  - 测试节点状态实时更新
  - 测试执行失败时的错误记录
  - **需求参考**：NFR-4（可测试性）

### 25. 实现 Todolist 执行暂停和恢复机制
- [ ] 在 `DAGStrategy` 中添加暂停检查点
  - 在每个节点执行前检查 Todolist 执行状态
  - 如果状态为 Paused，停止执行
  - 保存当前执行进度
  - **需求参考**：US-9（工作流暂停和恢复），R-9.3（断点恢复）
- [ ] 在 `DAGStrategy` 中实现恢复逻辑
  - 加载 Todolist 执行状态
  - 跳过已完成的节点
  - 从断点继续执行
  - **需求参考**：US-9（工作流暂停和恢复），R-9.3（断点恢复）
- [ ] 编写集成测试
  - 测试执行中暂停
  - 测试从断点恢复
  - 测试已完成节点跳过
  - **需求参考**：NFR-4（可测试性）

---

## 第八阶段：UI 可视化组件 (P1)

### 26. 创建 Todolist 执行历史列表组件
- [ ] 创建 `TodolistHistoryList.razor` Blazor 组件
  - 显示 Todolist 执行列表（App 名称、状态、开始时间、持续时间）
  - 实现过滤（按状态、App）
  - 实现分页
  - 点击查看详情
  - **需求参考**：US-8（工作流执行历史查询），R-8.4（UI 展示）
- [ ] 编写组件测试
  - 测试列表渲染
  - 测试过滤功能
  - 测试分页功能
  - **需求参考**：NFR-4（可测试性）

### 27. 创建 Todolist 执行详情和依赖图组件
- [ ] 创建 `TodolistExecutionDetails.razor` Blazor 组件
  - 显示 Todolist 执行详细信息
  - 显示 Todolist 原始任务
  - 显示节点执行状态列表
  - **需求参考**：US-8（工作流执行历史查询），R-8.4（UI 展示）
- [ ] 创建 `DependencyGraph.razor` Blazor 组件
  - 使用 Mermaid.js 渲染依赖图
  - 节点状态颜色标记（绿色=完成、蓝色=运行中、红色=失败、灰色=待执行）
  - 支持点击节点查看详情
  - **需求参考**：US-7（查看依赖关系图），R-7.1（DAG 可视化）
- [ ] 编写组件测试
  - 测试详情页渲染
  - 测试依赖图渲染
  - 测试节点状态颜色
  - **需求参考**：NFR-4（可测试性）

### 28. 创建实时进度更新组件（可选）
- [ ] 创建 `TodolistProgressMonitor.razor` Blazor 组件
  - 使用轮询（或 SSE）获取 Todolist 执行状态
  - 实时更新节点执行进度
  - 显示当前执行节点
  - **需求参考**：NFR-2（可观察性）
- [ ] 配置轮询间隔
  - 从 appsettings.json 读取 `UpdatePollingIntervalMs`
  - 默认 2000ms
  - **需求参考**：NFR-3（可扩展性）

---

## 第九阶段：端到端集成测试 (P0)

### 29. 编写完整的端到端测试
- [ ] 创建 `TodolistWorkflowE2eTests.cs` 测试类
  - 测试场景 1：创建 Todolist → 转换 DAG → 执行 → 持久化
    - 模拟用户请求创建待办列表
    - 验证 DAG 转换成功
    - 验证 Todolist 执行
    - 验证数据库记录
  - 测试场景 2：Todolist 执行暂停和恢复
    - 创建并启动 Todolist 执行
    - 暂停执行
    - 验证状态为 Paused
    - 恢复执行
    - 验证完成
  - 测试场景 3：失败节点重试
    - 创建包含失败节点的 Todolist 执行
    - 验证失败状态
    - 重试失败节点
    - 验证重试成功
  - 测试场景 4：App Tool 调用
    - 创建 Tool App
    - 从 Agent 调用 Tool App
    - 验证参数传递和响应
  - **需求参考**：所有用户故事（US-1 到 US-13）

### 30. 性能和负载测试
- [ ] 创建 `TodolistWorkflowPerformanceTests.cs` 测试类
  - 测试大规模 DAG（100 节点）
  - 测试并发执行（10 个 Todolist 执行）
  - 测试 JSONB 查询性能
  - 测试 HybridCache 命中率
  - 验证性能指标（执行开销 < 10%）
  - **需求参考**：NFR-1（安全性），NFR-2（可观察性）

---

## 第十阶段：文档和配置 (P2)

### 31. 更新配置文件和依赖注入
- [ ] 更新 `appsettings.json`
  - 添加 HybridCache 配置
  - 添加 TodolistWorkflow 配置
  - 添加 ToolManagement 配置
  - **需求参考**：NFR-3（可扩展性）
- [ ] 更新 `Program.cs` 依赖注入
  - 注册所有新服务（ToolMetadataService, TodolistPersistenceService 等）
  - 配置 HybridCache
  - 添加启动初始化逻辑
  - **需求参考**：NFR-3（可扩展性）

### 32. 添加日志和监控指标
- [ ] 在所有核心服务中添加详细日志
  - ToolMetadataService（扫描、缓存命中/未命中）
  - TodolistConverter（任务映射、验证）
  - TodolistPersistenceService（创建、更新、查询）
  - DAGStrategy（节点执行、状态变更）
  - **需求参考**：NFR-2（可观察性）
- [ ] 添加性能计数器
  - todolist_executions_total
  - todolist_execution_duration_seconds
  - node_execution_failures_total
  - tool_app_invocations_total
  - tool_metadata_cache_hits_total
  - tool_metadata_cache_misses_total
  - **需求参考**：NFR-2（可观察性）

### 33. 创建 API 文档和示例
- [ ] 更新 README.md
  - 添加 DAG 工作流持久化功能说明
  - 添加配置指南
  - 添加使用示例
  - **需求参考**：NFR-4（可测试性）
- [ ] 创建 Postman 集合（可选）
  - 工具调用示例
  - Todolist 执行管理示例
  - **需求参考**：NFR-4（可测试性）

---

## 任务完成标准

所有任务完成后，系统应满足以下验收标准：

### 功能验收
- [ ] 所有 13 个用户故事（US-1 到 US-13）均已实现并通过测试
- [ ] 8 个工作流管理工具可通过 `/v1/chat/completions` 调用
- [ ] App Tool 可以作为可重用工具被其他 Agent 调用
- [ ] Todolist 可以成功转换为 DAG 并执行
- [ ] Todolist 执行历史可以持久化并查询
- [ ] Todolist 执行可以暂停、恢复和重试失败节点

### 质量验收
- [ ] 所有单元测试通过（覆盖率 > 80%）
- [ ] 所有集成测试通过
- [ ] 端到端测试覆盖所有主要用户场景
- [ ] 性能测试满足非功能需求（执行开销 < 10%）
- [ ] 代码符合项目规范（C# 编码规范）

### 非功能验收
- [ ] 所有配置项可通过 appsettings.json 修改
- [ ] 日志记录完整且详细
- [ ] 性能指标可通过监控系统查看
- [ ] 数据库迁移可以成功应用和回滚
- [ ] HybridCache 正常工作（命中率 > 90%）

---

**任务总数：** 33 个主任务（包含多个子任务）  
**估计时间：** 6-8 个工作日  
**依赖关系：** 任务按阶段顺序执行，每个阶段依赖前一阶段完成  
**风险点：** DAGStrategy 集成可能需要重构现有代码，Mermaid.js 依赖图渲染可能需要调试
